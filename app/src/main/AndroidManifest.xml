<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whatsapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_applogo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_applogo_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsApp"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".MessageActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.WhatsApp2" />

        <activity android:name=".RegisterActivity" />

        <activity android:name=".QuestionRegisterActivity" />

        <activity android:name=".HashTagRegisterActivity"/>

        <activity android:name=".Login_Activity" />


        <activity
            android:name=".LoadingActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>